{
  "name": "nimstamp",
  "private": true,
  "version": "0.1.0",
  "description": "Self-serve loyalty stamp cards inside Nimiq Pay",
  "license": "MIT",
  "packageManager": "pnpm@10.14.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "dev:web": "pnpm --filter @nimstamp/web dev",
    "dev:api": "pnpm --filter @nimstamp/api dev"
  }
}
